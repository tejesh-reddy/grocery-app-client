import React from 'react';
import { makeStyles } from '@material-ui/core';

export const CartListingStyles = makeStyles((theme) => {

    return {
        root: {
            padding: '1em',
            margin: '0 auto'
        },
        item: {
            marginBottom: '0.5em',
        }
    }
});
